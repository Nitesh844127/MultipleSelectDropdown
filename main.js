$(document).ready(function(){
   
    var multipleCancelButton = new Choices('#removeChoiceBtn', {
       removeItemButton: true,
       maxItemCount:5,
    //    searchResultLimit:5,    //Appear dropdown option size
    //    renderChoiceLimit:5   //Dropdown option size
     }); 
    
    
});